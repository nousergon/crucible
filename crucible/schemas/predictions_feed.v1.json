{
  "$id": "https://github.com/nousergon/crucible/schemas/predictions_feed.v1.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "The M champion's serving feed for one NYSE trading day, written at predictions/{trading_day}.json and read by the trader beside champions/m/current.json (plan \u00a73). It is a REPUBLICATION of exactly one arm_predictions.v1 document \u2014 the one the champion pointer resolves to \u2014 never an independently computed cross-section: the serving path resolves the pointer, it never imports a ranking function directly. `source_key` names the document republished so the trader's read and the harness's grade are provably the same numbers. additionalProperties: false because a field this reader does not understand is a field the producer expected it to act on.",
  "properties": {
    "champion": {
      "description": "The arm the pointer named when this feed was written. Its id encodes its own spec hash (policy \u00a73.1), so the trader can always tell which recipe produced the numbers it is about to size.",
      "minLength": 1,
      "title": "Champion",
      "type": "string"
    },
    "feature_version": {
      "description": "The feature-layer vintage the champion's fit read, carried through from the republished arm_predictions.v1 document. Without it a feed cannot be reproduced from the store it was written against.",
      "minLength": 1,
      "title": "Feature Version",
      "type": "string"
    },
    "predicted_alpha": {
      "additionalProperties": {
        "type": "number"
      },
      "description": "Name -> predicted alpha, the champion's cross-section for this session. Never empty: a feed with no names is not an empty opinion, it is a producer that failed and wrote anyway, and the trader must page rather than flatten the book on it.",
      "minProperties": 1,
      "title": "Predicted Alpha",
      "type": "object"
    },
    "schema_version": {
      "const": "predictions_feed.v1",
      "description": "Version of THIS schema. A consumer that cannot read the version refuses the document rather than guessing.",
      "title": "Schema Version",
      "type": "string"
    },
    "slot": {
      "const": "m",
      "description": "The slot whose champion this feed serves. Closed to `m`: `predictions/{trading_day}.json` is the M contract specifically, and R's and U's champions serve their own keys (`signals_key`, `universe_members_key`).",
      "title": "Slot",
      "type": "string"
    },
    "source_key": {
      "description": "The arm_predictions.v1 key this feed republishes. The hop `crucible explain` walks from the trader's read back to the arm's own artifact and from there to the run that fitted it.",
      "minLength": 1,
      "title": "Source Key",
      "type": "string"
    },
    "trading_day": {
      "description": "The NYSE trading day this cross-section is for (\u00a74.12). Repeated in the body as well as in the key so a misfiled feed is detectable by the reader rather than only by the path it was found at.",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$",
      "title": "Trading Day",
      "type": "string"
    }
  },
  "required": [
    "schema_version",
    "slot",
    "trading_day",
    "champion",
    "feature_version",
    "source_key",
    "predicted_alpha"
  ],
  "title": "Crucible champion predictions feed, v1",
  "type": "object"
}
