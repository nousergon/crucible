## What & why

<!-- Reference a tracked issue (alpha-engine-config-I<N>, since that is this
     repo's tracker), a concrete observed defect, or a named ruling. -->

## Test plan

<!-- Confirm the suite passed BEFORE opening this PR — "will verify in CI" is
     not acceptable. `uv run pytest -q --ignore=tests/acceptance` is the
     blocking suite; `tests/acceptance` is expected red until phase-1 exits. -->

## Deploy mechanism

**Deploy-mechanism:** <!-- name the workflow that fires on merge (e.g.
`deploy.yml`), or write `N/A` if this PR touches no deploy-triggering path. -->

---

Prepared by: <model-name> via [Claude Code](https://claude.com/claude-code)
